# REQ-003 生产部署清单 · 开票 Job（TODO-009）

**更新**：2026-07-05  
**代码仓**：`E:\Workspace\extend_platform`

---

## 1. 部署包

| 文件 | 说明 |
| --- | --- |
| `sql/req003_invoice_production_deploy.sql` | **合并脚本**：`uk_fpqqlsh` + 首轮开票 Job + 开票中轮询 Job |
| `sql/req003_invoice_production_deploy_verify.sql` | 部署后只读验证 |

单文件来源（内容已合并，勿重复执行）：

- `sql/recon_bill_invoice_fpqqlsh_uk.sql`
- `sql/recon_bill_invoice_issue_approved_job.sql`
- `sql/recon_bill_invoice_query_pending_job.sql`

---

## 2. 执行前检查

1. **etcplus-job-report** 已部署，且能 Feign 调用 etcplus-system 的 `/reconBillInvoice/*`。
2. 备份 `sys_job`、`etcplus_recon_bill_invoice`。
3. 确认无重复 `fpqqlsh`：

```sql
SELECT fpqqlsh, COUNT(*) FROM etcplus_recon_bill_invoice
WHERE fpqqlsh IS NOT NULL AND fpqqlsh != ''
GROUP BY fpqqlsh HAVING COUNT(*) > 1;
```

有重复须先清理，再执行 `uk_fpqqlsh`。

---

## 3. 执行步骤

```bash
mysql -h <host> -u <user> -p <database> < sql/req003_invoice_production_deploy.sql
mysql -h <host> -u <user> -p <database> < sql/req003_invoice_production_deploy_verify.sql
```

期望：`uk_fpqqlsh` 存在；两条 `sys_job` 且 `status=0`。

---

## 4. 部署后冒烟

1. 财务复审通过测试申请 → **2 分钟内** 出现 `etcplus_recon_bill_invoice`，`fpqqlsh` 以 `LHDF` 开头。
2. 开票中 → 1 分钟内轮询 Job 更新状态。

---

## 5. 状态

| 项 | 状态 |
| --- | --- |
| 合并 SQL 入库 | ✅ 2026-07-05 |
| 目标环境已执行 | ⬜ 待运维在预发/生产执行 verify |
